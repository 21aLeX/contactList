import React from 'react';
import s from './StyleComponents.module.css'

const Back = () => {
    return (
        <a className={s.back}>
            Назад
        </a>
    );
};

export default Back;