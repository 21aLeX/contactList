import React from 'react';
import s from './StyleComponents.module.css'

const Next = () => {
    return (
        <a className={s.next}>
            Вперед
        </a>
    );
};

export default Next;